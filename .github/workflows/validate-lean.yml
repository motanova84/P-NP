name: Validate Lean 4 Proofs

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Lean 4
        run: |
          curl https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh -sSf | sh -s -- -y
          echo "$HOME/.elan/bin" >> $GITHUB_PATH

      - name: Build Lean project
        run: |
          lake update                    # ⬅️ obtiene Mathlib4
          lake exe cache get mathlib     # ⬅️ descarga cache precompilada
          lake build                     # ⬅️ compila todo tu proyecto
